# FormFill MCP — Python dependencies
# Install with: pip install -r requirements.txt

# MCP server framework
mcp==1.26.0

# PDF reading and writing
pypdf==6.9.2

# Stripe billing (webhook + checkout)
stripe==14.4.1

# HTTP requests (transitive dep of stripe, pinned explicitly)
requests==2.32.5

# HTTP server for Railway deployment (health endpoint + ASGI wrapper)
uvicorn==0.42.0
starlette==1.0.0
